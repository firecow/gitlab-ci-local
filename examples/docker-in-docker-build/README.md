# docker in docker build example

#### !!! Important !!!
See how the project is configured in [.gitlab-ci-local-env](.gitlab-ci-local-env)


#### Start by calling.
```bash
gitlab-ci-local --cwd examples/docker-in-docker-build/
```
