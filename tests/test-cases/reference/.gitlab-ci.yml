---
include:
  - local: .gitlab-ci-base.yml

test-job:
  variables:
    MYVAR: !reference [.setup, variables, MYVAR]
  script:
    - !reference [.jibof2, script]
    - echo ${MYVAR}

.jibof2: # Job in 2nd bottom of file
  script:
    - !reference [.jibof, script]

.jibof: # Job in bottom of file
  script:
    - !reference [.setup, script]