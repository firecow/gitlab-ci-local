---
test-debian:
  image: debian:stable-slim
  script:
    - cat
    - cd folder
    - cat < text.txt

test-alpine:
  image: alpine
  script:
    - cat
    - cd folder
    - cat < text.txt

test-shell:
  script:
    - cat
    - cd folder
    - cat < text.txt
