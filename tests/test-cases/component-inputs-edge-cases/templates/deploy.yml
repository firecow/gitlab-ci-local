---
spec:
  inputs:
    environment:
      type: string
      default: dev
    replicas:
      type: number
      default: 1
---
deploy-$[[ inputs.environment ]]:
  stage: deploy
  script:
    - echo "Deploying to $[[ inputs.environment ]]"
    - echo "Replicas $[[ inputs.replicas ]]"
