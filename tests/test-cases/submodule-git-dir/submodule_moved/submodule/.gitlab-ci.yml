---
stages:
 - test

build-job:
 variables:
  GIT_SUBMODULE_STRATEGY: recursive
 stage: test
 script:
  - test -f .git/config || exit 1
  - grep -e "worktree" .git/config && exit 1
  - echo gitdir and worktree are ok
