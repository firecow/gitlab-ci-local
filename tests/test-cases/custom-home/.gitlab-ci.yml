---
test-job:
  script:
    - echo ${GLOBAL_VAR}
    - echo ${GROUP_VAR}
    - echo ${PROJECT_VAR}
    - cat ${GLOBAL_FILE_VAR}

test-normalize-key:
  script:
    - echo ${GROUP_VAR}
    - echo ${PROJECT_VAR}

test-predefined-overwrite:
  script:
    - echo ${CI_PIPELINE_SOURCE}
    - echo ${CI_PROJECT_ID}

