---
workflow:
  rules:
    - if: $NONEXISTENT_VAR == 'something'
      variables:
        WORKFLOW_VAR: from-workflow-rules

test-job:
  stage: test
  script:
    - echo "WORKFLOW_VAR=${WORKFLOW_VAR}"
