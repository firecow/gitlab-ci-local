---
include:
  - component: gitlab.com/components/go/full-pipeline@0.3.1
    inputs:
      stage_format: format
      stage_build: build
      stage_test: test
      go_version: latest

stages:
  - format
  - build
  - test
  - latest
