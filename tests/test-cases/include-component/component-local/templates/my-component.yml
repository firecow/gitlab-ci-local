---
spec:
  inputs:
    stage:
      default: test
---
component-job:
  script:
    - echo job 1 from $[[ component.name ]] ($[[ component.version ]]/$[[ component.reference ]]).
    - Sha is $[[ component.sha ]]
  stage: $[[ inputs.stage ]]
